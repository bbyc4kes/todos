import React from 'react';
import TodoList from '../components/todo-list/todo-list.component';

const TodoListPage = (): React.ReactNode => {
	return (
		<>
			<TodoList />
		</>
	);
};

export default TodoListPage;
